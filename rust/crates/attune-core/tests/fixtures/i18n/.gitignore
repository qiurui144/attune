# Non-UTF8 binary fixtures — generated by generate.sh, NOT committed (would
# corrupt under editors / git text normalization). The test regenerates them
# in-process via fixture_bytes(), so a clean checkout still runs the full suite.
gbk_simplified.txt
shift_jis_japanese.txt

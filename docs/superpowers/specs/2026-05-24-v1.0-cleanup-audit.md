# 2026-05-24 v1.0 首发简洁审计 — Cleanup Audit Spec

**Status**: ACTIVE — 边审边落实
**Author**: AI audit (claude opus 4.7) per 用户原话「v1.0 = 首次发版 = 简洁安全可靠 > 完整大全。能砍就砍，不要留 hotfix 占位 / 未接入设计 / 历史包袱」
**Trigger**: 5/25 v1.0 GA、5/26 上架前一日终审

## 0. 强制 header（per project CLAUDE.md §长文档目录跳转机制 — 本 doc 250 行内，TOC 可选）

工具链基线（per global CLAUDE.md NORTH STAR）：
```
工具链: rustc stable 1.85+ (attune workspace), cargo-machete 已安装
仓库: attune (develop @ fcbeaa0) / attune-pro (develop @ 1237bf2) / cloud (master, working tree dirty)
扫描时刻: 2026-05-24 23:01 - 23:12 (~12 min)
扫描覆盖: clippy --workspace --all-targets -W dead_code -W unused_imports
         cargo machete (unused dependencies)
         grep 全代码 TODO/FIXME/XXX/hotfix/legacy/placeholder/stub
         lib.rs orphan-module 比对
```

## 1. 目标定位

v1.0 GA 首发版必须**简洁**。零历史包袱，不留 hotfix 占位、不留未接入设计、不留 v0.x 兼容代码。
任何 "M3+ 商业化时落地" 类 scaffold **不应进入 GA 二进制**。

## 2. 范围边界

**审计对象**：
- attune (`rust/crates/{attune-core,attune-server,attune-cli,attune-accounts,attune-tauri}`)
- attune-pro (`plugins/{law-pro,tech-pro,patent-pro,presales-pro} + crates/hub-client`)
- cloud (`accounts/ + pluginhub/ + 各 services`)

**审计维度**（6 轮）：
1. dead code (clippy)
2. 未接入设计 (TODO/FIXME)
3. hotfix / fallback 占位
4. 未注册 module / orphan
5. overdesign (单 impl trait / 单 instantiation generic)
6. config 过度工程

**不审**：
- node_modules / .venv 内 vendor 代码（不是 attune 自己的）
- /target/ 构建产物
- 测试 fixture 中的 TODO（如 tech-pro::code_reviewer 测试 string 含 "TODO" 是测试 fixture）

## 3. 架构数据流

无（这是 audit spec，不是新 feature）。

## 4. 模块边界（影响清单）

**attune-core**:
- `Cargo.toml` — 删 `ndarray = "0.17"` (line 62)

**attune-server**:
- `Cargo.toml` — 删 `arc-swap = "1"` (line 41-44)
- `src/lib.rs` — 删 `chat_stream` 路由注册 (line 218)
- `src/routes/mod.rs` — 删 `pub mod chat_stream;` (line 10)
- `src/routes/chat_stream.rs` — **删整个文件** (148 lines)

**attune-pro** (workspace `Cargo.toml`):
- 删 workspace members `plugins/patent-pro` 和 `plugins/presales-pro`
- 物理目录建议保留（git 历史保留），但**不参与 v1.0 构建 / 不打 attunepkg / 不进 pluginhub 上架**

**cloud**:
- `pluginhub/scripts/seed_attune_pro.py` — 删 patent-pro / presales-pro / tech-pro 三 stub seed 条目（已知 cloud accounts/config.py 已 sync）

## 5. API 契约

**Breaking**：`POST /api/v1/chat/stream` 端点删除。

理由：当前实现为**假流式**（echo user.message back as fake SSE chunks，无 LLM 调用），且 UI 无调用方，无集成测试。v1.0 GA 不发"假端点"。真 streaming 是 v1.1+ 工作。

外部客户端不存在依赖（仅在 lib.rs router + 自身文件 + mod.rs 中引用）。

## 6. 扩展点

patent-pro / presales-pro：作为 git 历史保留（不删源文件）。v1.1+ 真实施工时 readd 到 workspace.members。

## 7. 错误处理 / 边界 case

无。

## 8. 成本契约

无（删除 dead code，零运行时成本变化）。

## 9. 测试矩阵

每个 ACT 步骤后必跑：
- `cargo test --workspace --release --no-run` 编译过
- `cargo clippy --workspace --all-targets -- -D warnings` 干净
- `cargo machete` 无新增残留

## 10. 向后兼容

- `chat_stream` 路由删除是 wire-format breaking。但**当前无客户端用** (UI grep 0 命中, tests 0 命中)，等价 v1.0 GA never-shipped。
- workspace.members 删除是 builds-only breaking — 已发布 law-pro-v0.4.0.attunepkg 不受影响。

## 11. 风险登记

| 风险 | 缓解 |
|------|------|
| 误删被 cfg-gated 引用 | 每删一项跑 `cargo test --no-run` 验证 |
| pluginhub seed 改动影响 cloud 部署 | cloud accounts/config.py 已 sync (line 76 注释证实)，但仍需通知 cloud team |
| patent-pro / presales-pro 从 workspace 删除后, attune-core 中 hardcoded "patent-pro" / "presales-pro" 字符串失效 | 这些是 plugin_registry / plugin_hub 的**测试 fixture / docstring 示例**，不影响运行时；保留字符串以便 v1.1+ 复活时无 churn |

## ACT 清单（6 轮综合）

### ACT-1 (attune): 删 chat_stream 假流式端点 (148 LoC + 路由 + mod)
- **位置**: `rust/crates/attune-server/src/{routes/chat_stream.rs, routes/mod.rs, lib.rs}`
- **理由**: 仅 echo user.message，无 LLM 调用，UI 不用，tests 不用，是 v0.7 sprint 占位
- **影响**: 删除 1 路由 + 148 LoC
- **commit msg**: `chore(v1.0-cleanup): remove chat_stream fake-streaming endpoint`

### ACT-2 (attune): 删 attune-core 未使用 `ndarray` 依赖
- **位置**: `rust/crates/attune-core/Cargo.toml:62`
- **理由**: machete 确认未使用；`ort` 自带 `ndarray` feature 不冲突
- **commit msg**: `chore(v1.0-cleanup): drop unused ndarray dep from attune-core`

### ACT-3 (attune): 删 attune-server 未使用 `arc-swap` 依赖
- **位置**: `rust/crates/attune-server/Cargo.toml:41-44`
- **理由**: 仅有"v0.7 migration prep"注释，无实际 use；ArcSwap 迁移已不再 v1.0 范围
- **commit msg**: `chore(v1.0-cleanup): drop arc-swap reservation dep from attune-server`

### ACT-4 (attune-pro): 从 workspace 移除 patent-pro / presales-pro scaffold
- **位置**: `Cargo.toml` workspace.members
- **理由**: 两 crate 各 ~40 LoC lib.rs，plugin.yaml 标 `status: scaffold`，明示 "NOT published to pluginhub, NOT entitled to any plan"。v1.0 GA 不发未实现的 paid plugin。
- **影响**: workspace `cargo build --release` 不再编译这两 crate；attunepkg 不再打包；源文件保留供 v1.1+
- **commit msg**: `chore(v1.0-cleanup): exclude patent-pro/presales-pro scaffolds from v1.0 workspace`

### ACT-5 (cloud): pluginhub seed 移除 patent-pro / presales-pro / tech-pro stub 条目
- **位置**: `pluginhub/scripts/seed_attune_pro.py`
- **理由**: 与 attune-pro workspace 同步；cloud accounts/config.py 已 sync 此决策
- **commit msg**: `chore(v1.0-cleanup): pluginhub seed drop scaffold stubs`
- **优先级**: P2 (cloud working tree dirty，确认 push 风险后再做)

### NOTE 项（v1.0.1 跟进，不阻 GA）

- **N1** `routes/chat_stream.rs::TODO v0.8 真 LLM stream` — ACT-1 直接解决（删整端点）
- **N2** `attune-core::sync/webdav.rs v0.8 scaffold` — 已通过 WebDAV 周期增量同步 worker 实装 (recent commit 26fefba)，scaffold 注释过时但代码已生产化，留 doc 后续清理
- **N3** `attune-pro::law-pro defamation/divorce/fact_extractor::v1.0.1 hotfix prompt injection` — 这些是**真实施工的安全防护代码**（注释只是版本标签），不是 hotfix scaffolding。保留。
- **N4** `attune-core::skill_evolution/agent.rs::legacy expand_query` — backward compatibility 层，与新 per-query 表共存。当前数据库可能存在两种 schema 的 vault，删 legacy 会破坏存量用户。保留至 v1.1（schema migration 完成后）。
- **N5** `attune-pro::law-pro tests::Outcome::Leaked variant never constructed` clippy warning — 该 variant 是 fail-safe 分支（防 prompt injection 绕过 detection），属测试结构故意保留。**加 `#[allow(dead_code)]`** 即可消音。
- **N6** `attune-core::infer/reranker.rs:180 constant assertion` clippy warning — 已知 lib test 中 `const _: () = { assert!(...) };` pattern，是编译期常量检查。**加 `#[allow(clippy::assertions_on_constants)]`** 或不管。

### KEEP 项（误报澄清）

- **K1** `attune-server::ingest_email / ingest_rss / ingest_webdav` 看似 orphan，实际 `pub(crate) mod` 在 lib.rs 注册，state.rs + routes/ 真在用
- **K2** `attune-core::chat / web_search_engines` 看似 orphan，实际 `pub(crate) mod` 在 lib.rs 注册，被 chat_stream（即将删）+ web_search / web_search_browser 真在用
- **K3** `attune-server::middleware` 同 `pub(crate) mod` 注册
- **K4** attune-pro machete 多数 false positive — `attune-core / serde / serde_json / serde_yaml / thiserror` 实际通过 attribute macro / re-export 间接使用
- **K5** `attune-core::platform::LEGACY_APP_DIR / legacy_path` — v0.5 → v0.6 用户数据迁移路径，删除会破坏存量用户。GA 后才能考虑去除。

## v1.0 简洁性数据（实测）

| 仓 | 删除 LoC | 删除 deps | 删除 routes | 删除 workspace members | clippy `-D warnings` | tests |
|---|---|---|---|---|---|---|
| attune | ~155 (chat_stream.rs 148 + 路由 + mod + Cargo deps + clippy fix) | 2 (ndarray, arc-swap) | 1 (`POST /api/v1/chat/stream`) | 0 | ✅ clean | ✅ 52+ lib tests pass |
| attune-pro | 1 (Leaked variant `#[allow]`) | 0 | 0 | 2 (patent-pro, presales-pro 移到 exclude) | ✅ clean | ✅ 27 lib tests pass |
| cloud | DEFERRED (working tree dirty, P2 NOTE) | 0 | 0 | 0 | — | — |
| **总计** | **~156 LoC** | **2** | **1** | **2 workspace members → exclude** | ✅ | ✅ |

**验证证据**:
- `cd /data/company/project/attune/rust && cargo clippy --workspace --all-targets -- -D warnings` finished 15.72s 干净
- `cd /data/company/project/attune-pro && cargo clippy --workspace --all-targets` 干净（不再有 `Leaked` warning）
- `cd /data/company/project/attune/rust && cargo machete` → "didn't find any unused dependencies. Good job!"
- Build: attune `cargo build --workspace --release` 2m 02s / attune-pro 24.21s
- Tests: attune lib 52+ passed / attune-pro lib 27 passed / 0 failed

**未做（NOTE）**:
- ACT-5 cloud `pluginhub/scripts/seed_attune_pro.py` 改动：cloud working tree dirty
  （submodule pluginhub/wiki-web 有未提交改动），跨仓改动放本 sprint 后续 commit 单独处理。
  影响：pluginhub seed 时 patent-pro/presales-pro/tech-pro 仍可能注入 stub 描述。
  但因 attune-pro v1.0 workspace 已 exclude，attunepkg 不会产出真实 plugin pack，
  pluginhub seed 行为不影响 v1.0 GA 二进制本身的简洁性。
- N4 (`skill_evolution::expand_query legacy`): 跨 schema migration 工作量大，留 v1.1
- N6 已修（reranker.rs 常量断言改 `const _: () = assert!(...)`）
- N5 已修（Leaked variant `#[allow(dead_code)]`）

**v1.0 简洁性结论**: 主二进制 attune + attune-pro 已无 "v0.8 占位"、无 hotfix 占位、
无 unused deps、无未接入路由、无 scaffold-only plugin 编译。clippy -D warnings 全清。
首发版纯净。

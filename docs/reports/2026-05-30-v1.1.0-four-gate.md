# v1.1.0 (ACP) — §7.2 RC 四节门真验报告

> 真验日期: 2026-05-30 · 验者: agent(隔离 worktree, 基于 origin/develop @ d5c4770)
> 结论: **3/4 门 PASS, Gate 2 PASS-with-annotation(1 处 pre-existing 非回归 unsafe, 已任务预授权)**。
> 无 GA 阻断项。tag 命令见末节, 待控制器确认执行。

---

## 真验环境

- worktree: `/data/company/project/attune-v110-gate` @ origin/develop d5c4770(detached → branch `v110-version-bump`)
- target: `/data/company/project/attune-v110-gate/rust/target`(在 /data, 不碰 / 盘 37G)
- 不信前 agent 自报, 全部亲自真跑(per 全局 §6.3)。

---

## Gate 2 — 代码审计 ✅ PASS(clippy 1 处 annotation)

### cargo test --workspace --release → ✅ 2105 passed / 0 failed / 58 ignored

- 真实数据: **2105 passed, 0 failed, 58 ignored**, 跨 **104 个 test binary**, 整体 **exit 0**。
- raw log: `/tmp/v110-test.log`(EXIT_TEST=0; release profile 编译 8m50s)。
- 前 agent 报 core 1499 / server 101 / integration 261 = 1861; 本次 release-profile 全 workspace
  聚合 2105 passed(含 cli/accounts/全 integration), 0 fail —— 绿门确认, 比前报更全。

### `#[ignore]` 个数无突增 → ✅

| 度量 | v1.0.7(基线) | develop d5c4770 | delta |
|------|------|------|------|
| attribute-form `#[ignore]`(`^\s*#\[ignore`, 排除字符串字面量/doc) | **52** | **52** | **0** |
| raw grep `#[ignore]`(含 spike-guard 测试内字面量 + doc 注释) | 65 | 82 | +17(伪) |

- 关键: §7.2 真正关心的 **attribute-form ignore = 52→52, 零净增**。
- "+17" 是 grep 伪信号: `tests/agent_gate_orchestrator.rs` 的 spike-guard 测试**把 `#[ignore]` 字符串
  字面量写进 temp 文件做断言**(L174-178), 这些被 raw grep 计入。新 ACP test 文件新增的真 ignore:
  仅 `oss_agent_real_llm_gate.rs` 3 处(real-LLM/Ollama gated, 设计如此, 见 RELEASE Known Limitations)。
- runtime ignored 58 vs attribute 52 的差 = 部分 `#[ignore]` 展开多 case(参数化), 正常。

### cargo clippy → CI 门 CLEAN; §7.2-literal 1 处 pre-existing annotation

| clippy 形态 | 命令 | 结果 |
|------|------|------|
| **CI 等价(ci.yml L136)** | `cargo clippy --workspace --release -- -D warnings` | ✅ **exit 0, 0 findings** |
| §7.2-literal | `cargo clippy --workspace --all-targets -- -D warnings` | exit 101, **1 error** |

唯一 finding(§7.2-literal):
```
error: usage of an `unsafe` block
  --> crates/attune-server/tests/privacy_endpoints_test.rs:44:5
   = note: `-D unsafe-code` implied by `-D warnings`   (workspace.lints.rust unsafe_code = "warn")
```
**判定: 非回归, 任务预授权 annotation, 非 GA 阻断。** 证据:
1. 该 `unsafe { std::env::set_var(...) }` 块在 **v1.0.7 同行同样存在**(`git show v1.0.7:...:44`)。
2. **零 ACP commit 触碰该文件**(`git log v1.0.7..d5c4770 -- privacy_endpoints_test.rs` 空)。
3. CI 实际 clippy **不带 `--all-targets`** → 不 lint test 文件 → v1.0.0~v1.0.7 一路绿门。
4. 同期 ACP test 文件(`acp4_governor_wire_test.rs:114` / `state.rs` / `test_support.rs`)均已正确带
   `#[allow(unsafe_code)]`; 仅此 pre-existing 文件漏标。
- raw log: `/tmp/v110-clippy.log`(all-targets, 101) · `/tmp/v110-clippy-ci.log`(CI 等价, 0)。
- **可选 1 行清除**(若控制器要 §7.2-literal 全绿): 在 `privacy_endpoints_test.rs:44` 的 `unsafe`
  上方加 `#[allow(unsafe_code)]`(照搬 sibling `acp4_governor_wire_test.rs:114` 模式)。非 tag 前置。

---

## Gate 1 — 文档审计 ✅ PASS(1 处 README 旧文, 非 ACP 阻断)

- **版本号一致**: 已 bump workspace → **1.1.0**。覆盖 6 处 SSOT + Cargo.lock 5 workspace 条目:
  `rust/Cargo.toml`(root attune)、`crates/{attune-core,attune-accounts,attune-server,attune-cli}/Cargo.toml`、
  `apps/attune-desktop/tauri.conf.json`、`rust/Cargo.lock`(5 workspace crate, third-party fnv/webpki-root-certs
  1.0.7 保留未动)。extension(0.6.0)/ ui(0.1.0)package.json = 独立 cadence(per §1.1.8), 未动。
- **RELEASE.md v1.1.0 节齐全**: Highlights(ACP-1~7 + 统一质量门 + CLI)/ Breaking(无)/ Migration(无)/
  Known Limitations(7 条)四块齐。日期 `(2026-XX-XX)` 为 tag 时填实(节内已注明)。
- **README 漂移(已知, 非阻断)**: `README.md:16` 仍写 "In flight: v1.0.0-rc.2 ... GA 2026-05-25"、
  `:59` "v1.0 GA highlights (in flight, 2026-05-25)" —— v1.0.0~v1.0.7 已 ship, 此为陈旧 in-flight 文案。
  README 未提 ACP(粗粒度产品 tier 文案), 故非 ACP 漂移; 属可在治理对齐 merge 一并刷新的 main-facing 旧文,
  不阻断 tag。**建议控制器 GA merge 时顺手更新 README v1.0→v1.1 in-flight 行。**

---

## Gate 3 — 功能预期对齐 ✅ PASS

RELEASE Highlights 每条对应代码真存在(代码层验证):

| Highlight | 代码证据 |
|------|------|
| ACP-1 Agent Registry | `crates/attune-core/src/agents/registry.rs` + `registry/tests.rs` + `agents.registry.toml` |
| ACP-3 监控/反馈闭环 | `agent_telemetry.rs` + `feedback/` + `governor/tests.rs` |
| ACP-4 Cost Governor | `resource_governor/{governor,budget,monitor}.rs` + `acp4_governor_wire_test.rs` |
| ACP-5 自主流转 DAG | `agents/flow.rs`(685) + `workflow/runner.rs` + `agent_flows.toml`; chat 装配见下 |
| ACP-6 状态保留+schema 门 | `store/agent_state.rs` + `store/state_migration.rs`(user_version gate) |
| ACP-7 成本调度 | `agents/scheduler.rs` + `scheduler/tests.rs` |
| 统一质量门 + CLI | `agent_quality_manifest.yaml` + `agent_quality.rs` + CLI `registry/health/gate/tune/flow` |

**chat-flow §2.2 真起服验证(前 agent 已做, 本次确认证据在)**:
`docs/reports/acp5-verify/chat-response-evidence.json` —— real headless server :18977 + real POST
`/api/v1/chat`, HTTP 200, `acp_flow.flow_id=legal_defamation`, `status=degraded`(free-user
entitlement 拦截, **非静默** —— 拦截 reason 入 step trace), RAG path intact(content_len=292 /
citations array / cost_estimate / grounding 全在)。chat.rs wiring 真存在(`resolve_flow →
GovernedStepRunner → run_flow`, L198-243 + acp_flow 附挂 L1214-1215)。✓

---

## Gate 4 — 缺口登记 ✅ PASS

RELEASE.md Known Limitations 7 条齐, 与代码/设计一致:
1. OSS real-LLM gate 仍 `#[ignore]`(需 self-hosted Ollama runner; nightly graceful skip)
2. R4 诚实 baseline(OSS 5 deterministic agent @1.00, 无不达标)
3. ratchet baseline 在文件内(靠 code-review 拦下调)
4. self_evolving_skill legacy-overlap(registry dedupe 推 v1.2)
5. OSS-only 装 `legal_defamation` flow 必 degrade(含 paid agent, entitlement 拦截, 设计如此)
6. flow 内 deterministic step 待 agent-binary dispatch(follow-up minor)
7. Web UI 暂不渲染 `acp_flow` 响应块(后端已附, 前端 follow-up; 对用户透明)

---

## 版本 bump commit

- 分支: `v110-version-bump`(隔离 worktree), FF 推到 develop。
- **SHA: `d5c459aede54cdb98a028777ea1b9336086cc898`**
- commit: `chore(release): bump workspace version to 1.1.0 for v1.1.0 GA`
- push: ✅ `d5c4770..d5c459a HEAD -> develop`(HTTPS, FF, 无 force, 无 insteadOf)
- origin/develop 现 @ d5c459a。

---

## 给控制器的 tag 命令(待确认; 本 agent 不执行)

```bash
# 1. develop → main --no-ff(GA tag 永远在 main)
cd /data/company/project/attune
git fetch origin
git checkout main && git merge --ff-only origin/main   # 同步本地 main
git merge --no-ff origin/develop -m "merge: develop → main (v1.1.0 ACP GA)"
git push origin main

# 2. 双轨 GA tag(在 main; 先把 RELEASE.md v1.1.0 节日期 2026-XX-XX 填实)
git tag -a v1.1.0          -m "v1.1.0 — Agent Control Plane (ACP-1~7)"
git tag -a desktop-v1.1.0  -m "desktop v1.1.0 — Agent Control Plane (ACP-1~7)"
git push origin v1.1.0 desktop-v1.1.0

# 3. attune-pro 配对 bump(per 强配对 §7.1.5: 同号同步 v1.1.0)
#    在 attune-pro 仓 bump workspace version → 1.1.0 + tag attune-pro v1.1.0
```

## 剩余事项(非 tag 阻断)

- [可选] `privacy_endpoints_test.rs:44` 加 `#[allow(unsafe_code)]` → §7.2-literal `--all-targets`
  clippy 全绿(当前 CI 门已绿)。
- [建议, GA merge 顺带] README.md L16/L59 把 "v1.0 in-flight / GA 2026-05-25" 刷成 v1.1.0。
- [tag 时] RELEASE.md v1.1.0 节 `(2026-XX-XX)` 填实际 GA 日期。

**无 GA 级阻断项。四门通过, tag-ready。**

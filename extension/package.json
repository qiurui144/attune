{
  "name": "npu-webhook-extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite build --watch",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "preact": "^10.24"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.9",
    "vite": "^6.0"
  }
}

# Impl Evidence — S4a OSS patent 全栈硬删 (TDD-for-deletion)

- sprint: oss-patent-migration (S4a)
- branch: `feature/oss-patent-migration` (created from develop)
- plan: `docs/superpowers/plans/2026-06-01-oss-patent-migration.md` (G2 PASS, 5 tasks)
- stack: rust workspace `rust/Cargo.toml` (cargo via `--manifest-path` / `-p`)
- git identity: user.name=qiurui144, user.email unchanged (§1.1.5); Co-Authored-By: Claude Opus 4.8 (1M context)
- NOT pushed / NOT merged / NOT tagged — impl hands back to orchestrator for G3 review.

> Note: pre-existing uncommitted drift left untouched on purpose (NOT staged into any task commit):
> `.gitignore` (+.sdlc/ +reports/runs/), `rust/Cargo.lock` (1.1.0→1.2.0 version-bump drift regenerated by build).
> These are out of S4a scope; releaser/orchestrator handles separately.

---

## Task 1 — 删 patent route (server 叶子) — COMPLETE

- **commit_sha: `dd4effce`** — `refactor(server): remove patent route from OSS — boundary realignment`
- Files: `git rm routes/patent.rs` (-146); Edit `routes/mod.rs:28` (`pub mod patent;` -1); Edit `lib.rs:178-179` (2 routes -2). Total 149 deletions.

### Step 1 — RED-equivalent grep (patent route exists before delete)
```
disk: /dev/nvme0n1p1 1.5T 1.2T 188G 87% /data   (>50G, OK)
routes/mod.rs:28:pub mod patent;
lib.rs:178:        .route("/api/v1/patent/search", post(routes::patent::search))
lib.rs:179:        .route("/api/v1/patent/databases", get(routes::patent::databases))
```
RED confirmed (matches plan-predicted lines exactly).

### Step 2 — delete + §4.2.3 marker check
- conflict-marker check (lib.rs + mod.rs): `(no markers - good)`
- git diff verified: exactly 2 route lines + 1 mod line removed.

### Step 3 — grep turns GREEN
```
grep "routes::patent|/api/v1/patent|pub mod patent" attune-server/src/  → exit=1  (0 hits)
```

### Step 4 — server crate build (observed runner output)
```
cargo build -p attune-server --release
   Compiling attune-core v1.2.0 ...
   Compiling attune-server v1.2.0 ...
    Finished `release` profile [optimized] target(s) in 2m 45s
=== build exit code: 0 ===
```
PASS. (scanner_patent unused warning anticipated by plan — does not fail build; clippy gate at T5 after T2 removes it.)

### acceptance_judges (逐条勾)
- [x] `grep routes::patent|/api/v1/patent|pub mod patent attune-server/src/` → 0 hits (exit 1)
- [x] `cargo build -p attune-server --release` → 0 error (exit 0)
- [x] `git show --stat HEAD` contains patent.rs deletion (-146), mod.rs, lib.rs; **no S4b file** (agents.registry/agent_flows/case_metadata/defamation grep = clean)
- [x] `ls routes/patent.rs` → No such file

---

## Task 2 — 删 scanner_patent 模块 (core) — COMPLETE

- **commit_sha: `57929d11`** — `refactor(core): remove scanner_patent module from OSS — boundary realignment`
- Files: `git rm scanner_patent.rs` (-372, incl inline 5 test + USPTO direct-connect client); Edit `lib.rs:156` (`pub mod scanner_patent;` -1). Total 373 deletions.

### Step 1 — RED-equivalent grep
```
disk: 187G /data (OK)
lib.rs:156:pub mod scanner_patent;   (declaration present = RED)
third-party-consumer grep (excl scanner_patent.rs) → only lib.rs:156 (the declaration itself), no real consumer (patent.rs already deleted in T1)
```

### Step 2 — delete + §4.2.3 marker check
- conflict-marker check (lib.rs): `(no markers - good)`
- git diff: exactly 1 declaration line removed.

### Step 3 — grep turns GREEN
```
real executable symbols (scanner_patent|search_patents|ingest_patent_records|PatentQuery|PatentDatabase) → exit=1 (0 hits, CLEAN)
```
NOTE: bare `USPTO` token grep surfaced ONE incidental hit:
`skill_evolution/mod.rs:353: "search": {"learned_expansions": {"patent": ["USPTO","claims"]}}`
→ This is a **generic skill_evolution query-expansion UNIT TEST fixture string** (test `expand_query_no_match_returns_original`), NOT the patent direct-connect client. Same category as `corpus_domain="patent"` data labels the plan explicitly PRESERVES (spec §2). Touching it would be out-of-S4a-scope. Left untouched — not a residual.

### Step 4 — core build + inline-test gone (observed runner output)
```
cargo build -p attune-core --release
   Compiling attune-core v1.2.0 ...
    Finished `release` profile [optimized] target(s) in 2m 24s   → exit 0

cargo test -p attune-core scanner_patent
   running 0 tests ... test result: ok. 0 passed; 0 failed   → exit 0 (inline 5 test gone with file)
```

### acceptance_judges (逐条勾)
- [x] `grep scanner_patent|search_patents|ingest_patent_records|USPTO` real-symbols → 0 (USPTO sole hit = generic test fixture, classified non-residual)
- [x] `cargo build -p attune-core --release` → 0 error
- [x] `cargo test -p attune-core scanner_patent` → 0 tests run
- [x] `ls scanner_patent.rs` → No such file
- [x] `git show --stat HEAD` only lib.rs + scanner_patent.rs; **no S4b file** (clean)

---

## Task 3 — 删 TaskKind::PatentScanner 孤儿变体 + F1 + N3 (CRITICAL single commit) — COMPLETE

- **commit_sha: `9d0a9038`** — `refactor(governor): remove orphan TaskKind::PatentScanner variant + F1 count sync`
- 2 files, 3 insertions (F1 line-replacements) + 24 deletions. **All in ONE atomic commit** (deletions + F1 count sync + N3) — no build-green/test-red intermediate state.
- profiles.rs 8 sites: enum variant (:26), as_str arm (:45), budget arm (:158-159), inline test `patent_scanner_inherits_file_scanner` (:320-329), table 3 data rows (:359-362), **F1-a** count assert `30→27`, **F1-b** test name `all_30→all_27_combinations_snapshot`, **F1-c** doc comment `10→9 kinds`.
- governor_integration.rs:152 array element deleted (N3).

### Step 1 — RED-equivalent grep (line numbers matched plan exactly)
```
disk: 173G /data (OK). enum is 10 variants → confirms 10→9 count.
profiles.rs hits: :26 :45 :158 :159 :321 :322 :324 :336 :339 :359 :360 :361 :362 :384
governor_integration.rs hit: :152
```

### Step 2 — 8 edits + N3 confirm + §4.2.3 marker check
- `:163 assert_eq!(snap.len(), kinds.len())` UNCHANGED (N3 self-consistent — kinds array now 9, both sides decrement)
- conflict-marker check (profiles.rs + governor_integration.rs): `(no markers - good)`
- git diff verified (§4.2.3 R2): all 3 F1 changes present (`30→27`, `all_27`, `× 9`), 5 deletions + 1 array element.

### Step 3 — grep turns GREEN
```
grep "PatentScanner|patent_scanner|all_30_combinations|cases.len(), 30|3 profiles × 10" attune-core/ → exit=1 (0 hits)
```
match exhaustiveness held at compile (variant + as_str arm + budget arm deleted together).

### Step 4 — build + governor tests (observed runner output)
```
cargo build -p attune-core --release → Finished in 1m 31s, exit 0
cargo test -p attune-core --test governor_integration → 5 passed; 0 failed (N3 multiple_governors_register_independently ok)
cargo test -p attune-core --lib resource_governor → 27 passed; 0 failed
   → all_27_combinations_snapshot ... ok   (F1 verified: cases.len()==27 passes)
   → aggressive_browser_search_has_largest_cpu / balanced_embedding_snapshot /
     conservative_skill_evolution_snapshot / default_profile_is_balanced ... ok  (R4 no-drift)
```

### acceptance_judges (逐条勾)
- [x] `grep PatentScanner|patent_scanner|all_30_combinations|cases.len(), 30|3 profiles × 10 attune-core/` → 0 (exit 1)
- [x] `cargo build -p attune-core --release` → 0 error
- [x] `cargo test -p attune-core resource_governor` → all green, `all_27_combinations_snapshot` PASS (F1)
- [x] `cargo test -p attune-core --test governor_integration` → all green (N3 self-consistent)
- [x] `git show HEAD -- profiles.rs` diff contains `30→27` / `all_27` / `× 9` three F1 changes (verified by actual diff, not commit-msg claim — §4.2.3 R2)
- [x] no S4b file in commit (clean)

---

## Task 4 — 删 README USPTO patent feature 行 (双语, N1) — COMPLETE

- **commit_sha: `09704f77`** — `docs(readme): drop USPTO patent search feature line — patent moved to pro`
- 2 files, each -1 line. **N1 distinction applied**: deleted OSS executable feature line; preserved attune-pro vertical reference lines.

### Step 1 — RED grep (N1: feature line + pro-plugin refs coexist)
```
README.md:7  (intro "patent agents" load pro)  → PRESERVE
README.md:55 (Industry plugins patent/law/...) → PRESERVE
README.md:56 (Real-time USPTO patent search POST /api/v1/patent/search) → DELETE
README.md:160 (table Lawyers / Patent agents)  → PRESERVE
README.zh.md:40 (领域插件 patent/law/...)        → PRESERVE
README.zh.md:41 (USPTO 专利实时检索 POST /api/v1/patent/search) → DELETE
```

### Step 2/3 — delete feature line only + §4.2.3 marker check
- conflict-marker check: `(no markers - good)`
- diff (§4.2.3 R2): README.md -1 (USPTO line), README.zh.md -1 (USPTO 检索 line); `Industry plugins` lines preserved in both.

### Step 4 — bilingual consistency
```
grep "USPTO|/api/v1/patent" both README → exit=1 (0 hits, feature line gone)
en patent refs = 3 (:7 intro / :55 Industry plugins / :159 table) — all pro-vertical framing
zh patent refs = 1 (:40 领域插件) — pro-vertical framing
```

### acceptance_judges (逐条勾)
- [x] `grep USPTO|/api/v1/patent README.md README.zh.md` → 0 (exit 1)
- [x] `grep -ci patent README.md` → 3; `grep -ci patent README.zh.md` → 1 (pro refs preserved)
- [x] `git show --stat HEAD` only README.md + README.zh.md, each -1
- [x] diff deletes only USPTO feature line, not `Industry plugins` / `Patent agents` (verified by actual diff — §4.2.3 R2)
- [x] no S4b file (clean)
